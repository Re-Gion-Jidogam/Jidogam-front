export interface Place {
  pid: string;
  name: string;
  category: string;
  point: number;
  address: string;
  visitedDate: string;
  guidebookCount: string;
  // reviews 장소 리뷰, 별점(보류)
  // images 이미지(보류)
}
